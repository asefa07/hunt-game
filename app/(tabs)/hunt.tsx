import { View, Text } from "react-native";

export default function HuntTab() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center", padding: 24 }}>
      <Text>Select a hunt from the Feed tab to start scanning.</Text>
    </View>
  );
}
