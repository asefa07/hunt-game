import { View, Text } from "react-native";
export default function Hunt() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Hunt (QR scan + hints)</Text>
    </View>
  );
}
