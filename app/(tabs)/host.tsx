import { View, Text } from "react-native";
export default function Host() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Host (create hunt + upload)</Text>
    </View>
  );
}
