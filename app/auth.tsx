import { View, Text } from "react-native";

export default function Auth() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ fontSize: 18 }}>Auth Screen (next: Supabase)</Text>
    </View>
  );
}
